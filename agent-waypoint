#!/usr/bin/env bash
# Lightweight AgentWaypoint launcher.
# Delegates to scripts/agent-waypoint.mjs.

set -e
DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
exec node "$DIR/scripts/agent-waypoint.mjs" "$@"
